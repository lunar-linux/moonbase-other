depends mesa-lib
depends help2man

optional_depends bbswitch "" "" "for swithh on/off the discreete card"
optional_depends NVIDIA   "" "" "for the NVIDIA kernel driver"

#optional_depends NVIDIA-390xx "" "" "the NVIDIA kernel driver for old devices"

# optional depend on primus create a circular depend
#optional_depends prims "" "" "a faster back-end for optirun"
