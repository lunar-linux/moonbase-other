(

  cd unix                        &&
  ln -s Makefile.gtk Makefile        &&
  make                          &&
  prepare_install               &&
  make prefix=/usr install-strip
        

) > $C_FIFO 2>&1
