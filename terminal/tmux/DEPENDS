depends libevent
depends ncurses

optional_depends libutempter \
                 "--enable-utempter" \
                 "--disable-utempter" \
                 "for session logging in utmp and wtmp files"
