(

  # Some sane opts defaults
  OPTS+=" --enable-mousewheel \
          --enable-slipwheeling \
          --enable-keepscrolling \
          --enable-smart-resize \
          --enable-combining \
          --enable-utmp \
          --enable-wtmp \
          --enable-lastlog" &&

  default_build &&
  chmod 644 README* Changes INSTALL

) > $C_FIFO 2>&1

