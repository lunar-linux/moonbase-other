depends perl
depends ncurses

optional_depends "Linux-PAM"                                  \
                 "--enable-pam"                               \
                 "--disable-pam"                              \
                 "for Pluggable Authentication (PAM) support"
