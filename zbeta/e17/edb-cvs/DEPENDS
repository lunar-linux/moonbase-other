depends zlib &&

optional_depends "ncurses"  "--enable-ncurses"  ""    \
                 "build the ncurses based db editor"  &&

optional_depends "gtk+"     "--enable-gtk"      ""    \
                 "build the gtk+ (1.x) based db editor"
