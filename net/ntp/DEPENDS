depends readline
depends libedit

optional_depends libcap \
                 "--enable-libuxcaps" "" \
                 "use POSIX 1003.1e capabilities"

optional_depends %OSSL \
                 "--with-crypto" "" \
                 "use SSL crypting"
