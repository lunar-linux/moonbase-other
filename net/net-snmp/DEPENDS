depends %OSSL
depends libnl3
depends pciutils
depends python2-setuptools

optional_depends perl \
                 "--with-perl-modules --enable-embedded-perl --enable-shared" "" \
                 "Embed perl in the SNMP agent and install perl SNMP modules"
