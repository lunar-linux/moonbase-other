(

  prepare_install                                       &&
  cp -f gendiff.1.gz /usr/share/man/man1/gendiff.1.gz   &&
  cp -f gendiff /usr/bin/gendiff                        &&
  chmod +x /usr/bin/gendiff

) > $C_FIFO 2>&1
