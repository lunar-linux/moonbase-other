(

  cd unix                              &&
  ./configure --prefix=/usr             \
              --mandir=/usr/share/man  &&
  default_make                         &&
  ln -sf tclsh8.4 /usr/bin/tclsh    

) > $C_FIFO 2>&1
