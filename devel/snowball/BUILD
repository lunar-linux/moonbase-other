make &&

prepare_install &&

install -Dt /usr/bin snowball stemwords &&
install -Dm644 {.,/usr}/include/libstemmer.h &&
install -D {.,/usr/lib}/libstemmer.so.0.0.0 &&

ln -s /usr/lib/libstemmer.so.0 /usr/lib/libstemmer.so
