default_pre_build &&

# docbook build fails (likely due to our docbook module)
# but nobody will miss these if we just disable them here.
sedit 's/scons.1// ; s/scons-time.1// ; s/sconsign.1//' setup.cfg
