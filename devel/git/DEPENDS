depends zlib
depends curl
depends openssl
depends rsync
depends Python

if [ "$(get_module_config DOCS)" == "y" ]
then
    depends asciidoc
    depends xmlto
fi

optional_depends pcre \
                 "USE_LIBPCRE=1" "" \
                 "for perl regexp support in git-grep"

optional_depends gnome-keyring "" "" "for GNOME keyring credential helper"
optional_depends cvsps         "" "" "for CVS repository support"
optional_depends tk            "" "" "for a TK front-end git tool"
