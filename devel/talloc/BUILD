# override defaults, because else there would be conflicts with samba
OPTS+=" --includedir=/usr/include/$MODULE \
        --libdir=/usr/lib/$MODULE \
        --builtin-libraries=replace \
        --enable-talloc-compat1" &&

default_build
