depends npth
depends libgcrypt
depends libassuan
depends libksba
depends pinentry
depends sqlite
depends ncurses
depends bzip2

optional_depends curl          "" "" "for curl support"
optional_depends libusb-compat "" "" "for USB support"

optional_depends openldap \
                 "--with-ldap=/usr" \
                 "--disable-ldap"   \
                 "for LDAP support"

optional_depends readline \
                 "--with-readline=/usr" "" \
                 "for readline support"
