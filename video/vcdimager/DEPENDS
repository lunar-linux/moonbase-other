depends %PKGCONF
depends popt
depends libcdio

optional_depends libxml2 \
                 "--with-xml-frontend" \
                 "--without-xml-frontend" \
                 "to build xml frontend"
