optional_depends "openssl"            \
                 "--with-openssl"     \
                 "--without-openssl"  \
                 "for SSL support"

optional_depends "pygtk"              \
                 "--with-zenmap"      \
                 "--without-zenmap"   \
                 "for the Zenmap GUI"
