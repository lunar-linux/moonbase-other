depends pcre
depends libdnet
depends libpcap

optional_depends %OSSL \
                 "--with-openssl" \
                 "--without-openssl" \
                 "for SSL support"

optional_depends pygtk \
                 "--with-zenmap" \
                 "--without-zenmap" \
                 "for the Zenmap GUI"

#depends lua
