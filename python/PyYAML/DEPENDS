depends six

optional_depends setuptools "" "" "for Python 2.x support"

optional_depends yaml \
                 "--with-libyaml" \
                 "--without-libyaml" \
                 "for LibYAML support"
