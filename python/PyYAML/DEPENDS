depends setuptools

optional_depends Python-3 "" "" "for Python 3 support"

optional_depends yaml \
                 "--with-libyaml" \
                 "--without-libyaml" \
                 "for LibYAML support"
