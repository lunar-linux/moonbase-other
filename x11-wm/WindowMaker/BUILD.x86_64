(

  default_config  &&
  sedit '/ASM_X86/d' $SOURCE_DIRECTORY/src/config.h  &&
  default_make  &&

  if module_installed gdm; then
    cp $SCRIPT_DIRECTORY/WindowMaker /etc/X11/gdm/Sessions/
  fi

) > $C_FIFO 2>&1
