depends imlib2

optional_depends efl \
                 "--enable-eet" \
                 "--disable-eet" \
                 "to build the eet loader, ${PROBLEM_COLOR}Eet loader is broken, say no for now${DEFAULT_COLOR}"
