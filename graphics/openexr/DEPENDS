depends zlib

optional_depends fltk \
                 "-DOPENEXR_BUILD_VIEWERS=1" \
		 "-DOPENEXR_BUILD_VIEWERS=0" \
                 "to build the exrdisplay FLTK application, ${PROBLEM_COLOR}Viewers are currently out of order.${DEFAULT_COLOR}" n
