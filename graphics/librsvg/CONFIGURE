mquery GDKPIXBUF_LOADER "Enable GdkPixbuf loader?" y \
                        "--enable-pixbuf-loader" \
                        "--disable-pixbuf-loader"

mquery REBUILD_CAIRO    "Rebuild cairo upon completion to enable SVG testing?" y
