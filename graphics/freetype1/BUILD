(

  export CC="gcc"               &&
  ./configure  --prefix=/usr    \
               --enable-static  &&
  make                          &&
  prepare_install               &&
  make    install

) > $C_FIFO 2>&1
