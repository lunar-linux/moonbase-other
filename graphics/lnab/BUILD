(
  cd  $SOURCE_DIRECTORY                                 &&
  prepare_install                                       &&
  cp $SCRIPT_DIRECTORY/lnab /usr/bin/X11/lnab  &&
  chmod +x /usr/bin/X11/lnab

) > $C_FIFO 2>&1
