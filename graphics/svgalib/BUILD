(
  sedit "s/fix)\/man/fix)\/share\/man/" Makefile.cfg           &&
  make prefix=/usr                                             &&
  prepare_install                                              &&
  make prefix=/usr install

) > $C_FIFO 2>&1
