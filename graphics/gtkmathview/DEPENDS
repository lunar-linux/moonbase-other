depends libxml2
depends popt
depends %PKGCONF
depends t1lib
depends pangox-compat

optional_depends gtk+-2 \
                 "--enable-gtk" \
                 "--disable-gtk" \
                 "enable the Gtk+-2 backend"
