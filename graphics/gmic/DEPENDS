# these are all _technically_ optional,
# but g'mic won't do much without them

depends fftw
depends GraphicsMagick
depends openexr
depends opencv
depends eigen3
depends qt5

optional_depends gimp  "" "" "for the gimp gmic plugin"
optional_depends krita "" "" "for krita gmic plugin support"
