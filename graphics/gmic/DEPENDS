# these are all _technically_ optional,
# but g'mic won't do much without them
depends curl
depends openmp
depends fftw
depends GraphicsMagick
depends tiff
depends %JPEG
depends openexr
depends opencv
depends eigen3
