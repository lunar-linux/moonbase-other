# these are all _technically_ optional,
# but g'mic won't do much without them
depends curl
depends openmp
depends fftw
depends GraphicsMagick
depends tiff
depends %JPEG
depends openexr
depends eigen3

optional_depends opencv "-DENABLE_OPENCV=ON" "-DENABLE_OPENCV=OFF" "For realtime video support, ${PROBLEM_COLOR}Say no for now${DEFAULT_COLOR}"
