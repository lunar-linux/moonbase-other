kreqs="alsa-driver bluez-kernel cipe cryptoapi cvsfs drm-git fglrx ftpfs hostap-driver ieee80211 iptables ipw2100 ipw2200 ipw3945 i2c ndiswrapper ltmodem madwifi madwifi-svn fuse mwavem NFORCE NVIDIA NVIDIA-old NVIDIA-beta openafs-driver pcmcia-cs slmodem snare-core submount e1000"

for req in $kreqs; do
  if module_installed $req ; then
    # ignore failures as much as possible
    lin -c $req || true
  fi
done
