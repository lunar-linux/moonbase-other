(

  # shoot metaconfig in the head please:
  ./Configure -ders\
              -D gtkversion=2 \
              -D prefix=/usr \
              -D installbin=/usr/bin \
	      -D remotectrl=y &&
  default_make 

) > $C_FIFO 2>&1
