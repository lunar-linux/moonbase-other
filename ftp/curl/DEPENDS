depends zlib
depends libidn

optional_depends %OSSL \
                 "--with-ssl" \
                 "--without-ssl" \
                 "for SSL encrypted transport"

optional_depends libssh2 \
                 "--with-libssh2" \
                 "--without-libssh2" \
                 "for support of C implementation of ssh"

optional_depends krb5  "" "" "for Kerberos support"
