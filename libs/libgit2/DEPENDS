depends cmake
depends curl
depends %OSSL
depends zlib
depends libssh2

optional_depends http-parser \
                 "-DUSE_HTTPS=ON -DUSE_HTTP_PARSER=system" "" \
                 "for system installed http-parser support"
