depends autoconf-archive
depends python-setuptools
depends glib-2

optional_depends cython \
                 "" "--without-cython" \
                 "For Python bindings support using Cython, needed by libimobiledevice" \
                 "n"
