depends glib-2

# The --with-cython has to be left blank, else it gets confused and thinks you don't want it.
optional_depends "cython" "" "" "For Python bindings support using Cython, needed by libimobiledevice"
