default_pre_build &&

# libjxl stomps on zlib/usr/include/zconf.h and /usr/include/zlib.h
# so the BUILD is adjusted to install these includes elsewhere 
if module_installed libjxl; then 
  lrm libjxl &&
  lunar fix zlib
fi &&

./deps.sh
