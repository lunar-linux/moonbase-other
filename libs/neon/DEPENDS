depends %XMLRENDERER
depends %SSL

optional_depends "libproxy"             \
                 "--with-libproxy"      \
                 "--without-libproxy"   \
                 "for libproxy support"

optional_depends "heimdal"              \
                 "--with-gssapi"        \
                 "--without-gssapi"     \
                 "for heimdal support"
