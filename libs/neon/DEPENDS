depends %XMLRENDERER
depends %SSL

optional_depends "libproxy"             \
                 "--with-libproxy"      \
                 "--without-libproxy"   \
                 "for libproxy support"
