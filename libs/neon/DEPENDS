depends %XMLRENDERER
depends %SSL
depends zlib

optional_depends libproxy \
                 "--with-libproxy" \
                 "--without-libproxy" \
                 "for libproxy support" \
                 "n"

optional_depends heimdal \
                 "--with-gssapi" \
                 "--without-gssapi" \
                 "for heimdal support" \
                 "n"
