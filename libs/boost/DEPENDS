# For now making icu4c a depends until I or someone can figure out how
# to get boost to build/install properly without it.
depends bzip2
depends zlib
depends icu4c
depends zstd

optional_depends python  "" "--without-python" "to build the Boost python bindings" y
