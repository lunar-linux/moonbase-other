# The change in BUILD to unset OPTS and use default_python_build removes some conflicting files
# assimp used to install at the TLD of python. This does not absolve the need for a complete lunar fix.
lunar fix python
