depends zlib

optional_depends icu4c \
                 "--with-icu" \
                 "--without-icu" \
                 "for ICU support"

optional_depends python "" "" "to build Python bindings (required for GNOME)"
