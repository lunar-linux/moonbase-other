(

  . /etc/profile.d/qt4.rc

  default_make                        &&
  mkdir -p         /usr/share/PyQt-4  &&
  cp -R examples   /usr/share/PyQt-4  &&
  cp -R sip        /usr/share/PyQt-4

) > $C_FIFO 2>&1
