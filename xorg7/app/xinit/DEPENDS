depends xauth
depends xmodmap
depends xrdb
depends xorg-server

optional_depends "xterm"  "" "--without-xterm"  "for a very basic terminal (not needed by modern Desktop Environments)"
optional_depends "xclock" "" "--without-xclock" "for a very basic clock app (not needed by modern Desktop Environments)"
optional_depends "twm"    "" "--without-twm"    "for a very basic window manager (not needed by modern Desktop Environments)"
