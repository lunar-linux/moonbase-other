default_pre_build &&

cd $SOURCE_DIRECTORY/
unpack $SOURCE2 &&
mkdir -p cmake/modules &&
cp -fr cmake-$VERSION.src/Modules/* cmake/ &&

sedit '40i #include <cstdlib>' libomptarget/include/Debug.h &&
sedit '41i #include <string>' libomptarget/include/Debug.h 
