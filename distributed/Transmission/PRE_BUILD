# The source does not unpack into a dir, handle that here.
validate_source_dir $SOURCE_DIRECTORY  &&
mk_source_dir       $SOURCE_DIRECTORY  &&
cd $SOURCE_DIRECTORY                   &&
unpack $SOURCE
