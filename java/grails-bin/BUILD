(

  prepare_install &&
  mkdir -p /opt/lunar/grails &&

  cp -R ant bin build.properties build.xml conf dist lib media plugins scripts src /opt/lunar/grails/ &&
  chmod +x /opt/lunar/grails/bin/*

) > $C_FIFO 2>&1
