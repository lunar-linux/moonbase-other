(

  ( cd scintilla/gtk && make GTK2=1 )           &&
  ( cd scite/gtk     && make GTK2=1 )           &&
  prepare_install                               &&
  ( cd scite/gtk     && make GTK2=1 install )

) > $C_FIFO 2>&1
