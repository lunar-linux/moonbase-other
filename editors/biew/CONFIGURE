if  !  grep  -q  "USE_MOUSE="  $MODULE_CONFIG;  then

  if    query  "Do you want to enable mouse support?"  y
  then  echo  "USE_MOUSE=y"  >>  $MODULE_CONFIG
  else  echo  "USE_MOUSE=n"  >>  $MODULE_CONFIG
  fi

fi
