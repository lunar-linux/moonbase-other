depends autoconf	&&
depends automake	&&
depends Linux-PAM       &&
depends lsof		&&
optional_depends "heimdal" \
                 "--with-krb5-config=/usr/bin/krb5-config" \
	         "" \
                 "for kerberos support"

