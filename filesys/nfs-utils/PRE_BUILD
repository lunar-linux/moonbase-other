default_pre_build &&

# Fix rpc.statd starting
patch_it $SOURCE2 1 &&

# Do not wait for online target on statd
patch_it $SOURCE3 1 &&

# Put everything in /usr/bin
sedit 's;sbindir = /sbin;sbindir = /usr/bin;g' utils/*/Makefile.am &&

autoreconf -vfi
