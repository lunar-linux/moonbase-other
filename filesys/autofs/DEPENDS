depends libxml2

optional_depends "systemd"            \
                 "--with-systemd"     \
                 "--without-systemd"  \
                 "for systemd support"

optional_depends "openldap"            \
                 "--with-openldap"     \
                 "--without-openldap"  \
                 "for openldap support"
