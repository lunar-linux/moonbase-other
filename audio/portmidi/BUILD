default_build &&

# "linux API" support - some distributions seem to have separated
# the library and split of libporttime. Provide the library as redundant
ln -s /usr/lib/libportmidi.so /usr/lib/libporttime.so
