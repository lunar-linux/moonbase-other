optional_depends alsa-lib "--with-alsa" "--without-alsa" "for alsa audio support"
optional_depends jack2     "--with-jack" "--without-jack" "for low latency audio support,${PROBLEM_COLOR}Say NO on first install${DEFAULT_COLOR}"
