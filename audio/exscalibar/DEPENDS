depends libsndfile

# configure correctly detects that sndfile is not installed, but
# still tries to link against libsndfile, which fails. So just
# add the dependency until the upstream devs correct the bug.
