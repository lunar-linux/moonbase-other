(
  unset CC CXX                                              &&
  default_config                                            &&
  mkdir -p /usr/lib/python2.3                               &&
  cp -f $SOURCE_DIRECTORY/Tools/i18n/* /usr/lib/python2.3/  &&
  default_make

) > $C_FIFO 2>&1

