depends pcre
depends libxml2
depends zlib

optional_depends gettext \
                 "--with-gettext=shared" "" \
                 "for GNU gettext support"

optional_depends readline \
                 "--with-readline" "" \
                 "for readline support"

optional_depends aspell \
                 "--with-pspell" "" \
                 "for aspell spelling functions"

optional_depends enchant \
                 "--with-enchant=shared,/usr" "" \
                 "for enchant spelling functions"

optional_depends mhash \
                 "--with-mhash" "" \
                 "for hash functions support"

optional_depends mcrypt \
                 "--with-mcrypt=shared" "" \
                 "for crypto library"

optional_depends gmp \
                 "--with-gmp" "" \
                 "GNU math lib support"

optional_depends %OSSL \
                 "--with-openssl=shared" "" \
                 "for OpenSSL support"

optional_depends gdbm \
                 "--with-gdbm" "" \
                 "for GNU dbm support"

optional_depends db \
                 "--with-db4=/usr" "" \
                 "for Berkeley DB4 support"

optional_depends sqlite \
                 "--with-sqlite3=/usr --with-pdo-sqlite=shared,/usr" "" \
                 "for PDO SQLite support"

optional_depends %MYSQL \
                 "--with-mysql=/usr --with-mysqli=shared --with-pdo-mysql=shared,/usr" \
                 "" \
                 "for native MySQL support"

optional_depends postgresql \
                 "--with-pgsql=/usr --with-pdo-pgsql=shared,/usr" "" \
                 "for PostgreSQL support"

optional_depends unixODBC \
                 "--with-unixODBC=/usr --with-pdo-odbc=shared,/usr" "" \
                 "for unixODBC support"

optional_depends freetype2 \
                 "--with-freetype-dir=/usr" "" \
                 "for freetype2 support"

optional_depends libXpm \
                 "--with-xpm-dir=/usr" "" \
                 "for XPM pixmap support"

optional_depends gd \
                 "--with-gd=shared,/usr \
                  --enable-gd-native-ttf \
                  --with-jpeg-dir=/usr \
                  --with-png-dir=/usr" \
                 "" \
                 "external gd for on-the-fly graphics"

optional_depends curl \
                 "--with-curl=shared" "" \
                 "for cURL support"

optional_depends expat \
                 "--with-libexpat-dir=/usr" "" \
                 "for expat XML support"

optional_depends libxslt \
                 "--with-xsl=shared,/usr" "" \
                 "for DOM XSLT support"

optional_depends openldap \
                 "--with-ldap=shared" "" \
                 "for ldap support"

optional_depends libzip \
                 "--with-libzip=/usr" "" \
                 "for libzip support"

optional_depends bzip2 \
                 "--with-bz2=shared" "" \
                 "for bzip2 support"

optional_depends net-snmp \
                 "--with-snmp=shared" "" \
                 "for snmp (Simple Network Management Protocol) support"

optional_depends postfix \
                 "--with-imap=/usr" "" \
                 "for IMAP support"

optional_depends krb5 \
                 "--with-kerberos=/usr" "" \
                 "for Kerberos authentification support"

optional_depends httpd \
                 "--with-apxs2=/usr/sbin/apxs" "" \
                 "To build the apache2 DSO"
