depends perl
depends ninja
depends libssh2
depends sqlite
depends python
depends curl
depends libffi
depends jemalloc
depends musl

# It wants this also, and pulls in llvm
depends lld

optional_depends ccache "--enable-ccache" "--disable-ccache" "for cache support"
