
cd /usr/lib/gcc/$BUILD/$VERSION                          &&
ln    -sf /usr/bin/cpp cpp                               &&
cd /lib/                                                 &&
ln    -sf /usr/bin/cpp cpp                               && 

if [ ! -e /usr/bin/cc ] ; then
  ln -s gcc /usr/bin/cc
fi || true

