(

  default_pre_build               &&
  cd $SOURCE_DIRECTORY/ext        &&
  unpack $SOURCE3                 &&
  mv suhosin-$SUHOSIN_VER suhosin &&
  mv package.xml suhosin          
#  cd ..                           &&
#  patch_it $SOURCE4 1             &&
#  false

)

