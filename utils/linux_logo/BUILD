(   

  sedit 's/usr\/local/usr/' Makefile                           &&
  sedit 's/\$(DESTDIR)\/man/\$(DESTDIR)\/share\/man/' Makefile &&
  make logos-all                      &&
  prepare_install                     &&
  make install

) > $C_FIFO 2>&1



