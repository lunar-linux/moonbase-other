(

  make                                   &&
  prepare_install                        &&
  install -s mt     /usr/bin             &&
  install -s stinit /usr/sbin            &&
  install mt.1      /usr/share/man/man1  &&
  install stinit.8  /usr/share/man/man8

) > $C_FIFO 2>&1
