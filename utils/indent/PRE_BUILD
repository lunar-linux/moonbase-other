default_pre_build &&

patch_it $SOURCE_CACHE/$SOURCE2 1 &&

sedit 's/-number/-number-sections/' doc/Makefile.in &&

# -Os inlines is_reserved{,_cc} incorrectly with gcc
# that breaks the build... use current gperf instead
rm src/gperf.c src/gperf-cc.c &&
sedit '204 s/unsigned int/size_t/; 218 s/unsigned int/size_t/' src/lexi.c
