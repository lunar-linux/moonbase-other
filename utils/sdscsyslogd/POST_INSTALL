if ! $(id syslog &> /dev/null) ; then
     groupadd syslog &> /dev/null || true
     useradd -g syslog -d /var/log -s /bin/false syslog
fi


if ! [[ -e /etc/syslogd.conf ]]; then
  cp /usr/share/SDSCSyslogd/syslogd.conf.sample /etc/syslogd.conf
fi
