MODPATH=$MOONBASE/utils/$MODULE                  &&

mkdir -p $SOURCE_DIRECTORY                       &&
echo "cd $SOURCE_DIRECTORY"                      &&
cd $SOURCE_DIRECTORY                             &&
echo "cp $MODPATH/$MODULE.c $SOURCE_DIRECTORY"   &&
cp $MODPATH/$MODULE.c $SOURCE_DIRECTORY
