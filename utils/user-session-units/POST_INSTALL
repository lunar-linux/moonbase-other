message "Don't forget to select a default desktop and enable autologon:"
message "# ln -sf /usr/lib/systemd/user/<desktop>.target /etc/systemd/user/default.target"
message "# ln -sf /usr/lib/systemd/system/user-session@.service /etc/systemd/system/graphical.target.wants/user-session@<username>.service"
message "for more info, check the README"
