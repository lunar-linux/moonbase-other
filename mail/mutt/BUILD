OPTS=$OPTS" --enable-pop                     \
            --enable-imap                    \
            --with-mailpath=/var/spool/mail  \
            --with-docdir=/usr/share/doc/$MODULE"
(
   default_build
) > $C_FIFO 2>&1
