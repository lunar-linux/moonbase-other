depends bison

optional_depends "%OSSL" \
                 "--with-ssl=yes" \
                 "--without-ssl" \
                 "for SSL/TLS support"

optional_depends python-future "" "" "for fetchmailconf support" "n"
