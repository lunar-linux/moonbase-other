depends bison

optional_depends "%OSSL" \
                 "--with-ssl=/usr" \
                 "--without-ssl" \
                 "for SSL/TLS support"

optional_depends python-future "" "" "for fetchmailconf support"
