(

  ./configure  --with-emacs                                    \
               --with-lispdir=/usr/share/emacs/site-lisp/gnus  &&
  make                                                         &&
  prepare_install                                              &&
  make    install

) > $C_FIFO 2>&1
