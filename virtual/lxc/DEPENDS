depends Python
depends libcap
depends libseccomp
depends rsync

optional_depends lua \
                 "--enable-lua"  \
                 "--disable-lua" \
                 "for lua support"

optional_depends docbook2X \
                 "--enable-doc" \
                 "--disable-doc" \
                 "build man-pages"

