(

#  ./configure  --prefix=/usr         \
#               --localstatedir=/var  \
#               --sysconfdir=/etc     &&
#  make                               &&
#  prepare_install                    &&
#  make    install

CFLAGS="$CFLAGS -I/usr/include/libcapplet1"
default_build

) > $C_FIFO 2>&1
