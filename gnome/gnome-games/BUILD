(

  ./configure  --prefix=/usr        \
               --bindir=/usr/games  \
               --sysconfdir=/etc    &&
  make                              &&
  prepare_install                   &&
  make    install

) > $C_FIFO 2>&1
