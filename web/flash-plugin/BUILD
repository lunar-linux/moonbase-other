(

  cd $SOURCE_DIRECTORY                         &&
  prepare_install                              &&
  mkdir -p /opt/lunar/plugins                  &&
  cp *.xpt *.so /opt/lunar/plugins 

) > $C_FIFO 2>&1
