depends perl
depends zlib

optional_depends "openssl"       \
                 "--with-ssl"    \
                 "--without-ssl" \
                 "to link with OpenSSL"

optional_depends %MYSQL             \
                 "--with-mysql"     \
                 "--without-mysql"  \
                 "for support for mysql access"
