depends perl
depends zlib
depends expat

optional_depends "%OSSL"       \
                 "--with-ssl"    \
                 "--without-ssl" \
                 "to link with OpenSSL"

optional_depends %MYSQL             \
                 "--with-mysql"     \
                 "--without-mysql"  \
                 "for support for mysql access"
