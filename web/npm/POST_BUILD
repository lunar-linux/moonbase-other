# A dirty hack to allow npm touch the /us/etc/ directory
# It relax the /usr/etc restriction at install
sedit "s|/usr/etc:E|/usr/etc:N|" /usr/lib/lunar/plugins/postbuild-sanity.plugin

default_post_build &&

# return /usr/etc to the restriction at install
sedit "s|/usr/etc:N|/usr/etc:E|" /usr/lib/lunar/plugins/postbuild-sanity.plugin

:
