depends fltk1.3
depends %JPEG
depends libpng
depends zlib

optional_depends %OSSL \
                 "--enable-tls" \
                 "--disable-tls" \
                 "Enable SSL support? ${PROBLEM_COLOR}(This is ALPHA code and does not implement security!)${QUERY_COLOR}"
