(

  ./configure  --prefix=/usr           \
               --disable-paraguitest  &&
  make                                &&
  prepare_install                     &&
  make    install

) > $C_FIFO 2>&1
