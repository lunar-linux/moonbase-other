(

  make   DATADIR=/usr/share/xtux               &&
  prepare_install                              &&
  mkdir  -p /usr/share/xtux                    &&
  mv     data/* doc/ scripts/ /usr/share/xtux  &&
  mv     xtux tux_serv /usr/games

) > $C_FIFO 2>&1
