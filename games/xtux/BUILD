(

  make   DATADIR=/usr/share/games/xtux               &&
  prepare_install                                    &&
  mkdir  -p /usr/share/games/xtux                    &&
  mv     data/* doc/ scripts/ /usr/share/games/xtux  &&
  mv     xtux tux_serv /usr/games

) > $C_FIFO 2>&1
