(

  ./configure  --build=$BUILD            \
               --prefix=/usr             \
               --sysconfdir=/etc         \
               --localstatedir=/var      \
               --bindir=/usr/games       \
               --mandir=/usr/share/man  && 

  default_make

) > $C_FIFO 2>&1
