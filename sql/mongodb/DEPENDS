depends boost
depends scons
depends libpcap
depends pcre
depends zlib

optional_depends snappy  "" "" "for snappy compressor library support"

optional_depends openssl \
                 "--ssl" "" \
                 "for SSL support"

optional_depends cyrus-sasl \
                 "--use-sasl-client" "" \
                 "for SASL auth support in the client library"
