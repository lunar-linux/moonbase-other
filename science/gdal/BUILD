(
  
  ./configure  --sysconfdir=/etc        \
               --prefix=/usr            \
               --mandir=/usr/share/man  \
               --localstatedir=/var     \
               --enable-shared          \
               --disable-static         &&

  default_make

) > $C_FIFO 2>&1

