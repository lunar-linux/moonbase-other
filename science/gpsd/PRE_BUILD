default_pre_build &&

if in_depends $MODULE qt6-base ; then
  source /etc/profile.d/qt6.rc
  OPTS+=" qt_versioned=6"
fi &&

if in_depends $MODULE systemd ; then
  OPTS+=" systemd=yes"
   else
  OPTS+=" systemd=no"
fi
