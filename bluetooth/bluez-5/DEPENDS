depends alsa-lib
depends readline
depends dbus-glib
depends libical
depends psmisc

optional_depends cups \
                 "--enable-cups" \
                 "--disable-cups" \
                 "to enable cups printing support"

optional_depends %UDEV \
                 "" "--disable-udev --disable-systemd" \
                 "for udev/systemd support"
